{# @block content The dropdown header content. #}
<div
    class="{{ ('col-span-5 px-3.5 pt-2.5 pb-1 sm:px-3 ' ~ attributes.render('class'))|trim }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</div>
