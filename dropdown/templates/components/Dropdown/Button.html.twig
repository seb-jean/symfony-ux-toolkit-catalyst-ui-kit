{%- props
    ## string The component to render as.
    as = 'Button'
-%}
<twig:component is="{{ as }}" {{ ...attributes }}>
    {## The dropdown button. -#}
    {{ block(outerBlocks.content) }}
</twig:component>
