{# @block content The dropdown structure, typically includes `Dropdown:Button` and `Dropdown:Menu` #}
<el-dropdown
    class="{{ ('relative inline-block text-left ' ~ attributes.render('class'))|trim }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</el-dropdown>
