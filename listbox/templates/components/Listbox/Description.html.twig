<span
    {{ attributes.defaults({
        class: 'flex flex-1 overflow-hidden text-zinc-500 group-focus/option:text-white before:w-2 before:min-w-0 before:shrink dark:text-zinc-400',
    }) }}
>
    <span class="flex-1 truncate">
        {##- The description text. -#}
        {%- block content %}{% endblock -%}
    </span>
</span>
