{# @block content The checkbox fields. #}
<div
    class="{{ ('space-y-3 has-data-[slot=description]:space-y-6 has-data-[slot=description]:**:data-[slot=label]:font-medium ' ~ attributes.render('class'))|trim }}"
    {{ attributes.defaults({
        'data-slot': 'control',
    }) }}
>
    {%- block content %}{% endblock -%}
</div>
