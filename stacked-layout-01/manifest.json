{
    "$schema": "../../../schema-kit-recipe-v1.json",
    "type": "block",
    "name": "StackedLayout",
    "description": "You don't have enough features to fill a sidebar anyway, plus they aren't going to be cool forever.",
    "copy-files": {
        "templates/": "templates/"
    },
    "dependencies": {
        "recipe": ["button", "fieldset", "heading", "input", "text"]
    }
}
