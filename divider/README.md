# Divider

It's a line.

```twig {"preview":true}
<twig:Divider />
```

## Installation

::: installation

## Examples

### Basic example

Use the `Divider` component to add a horizontal rule between items:

```twig {"preview":true}
<twig:Divider />
```

### With reduced contrast

Use the `soft` prop for secondary dividers:

```twig {"preview":true}
<twig:Divider soft />
```

### With spacing

Use utility classes like `my-*` to control the vertical spacing around a divider:

```twig {"preview":true}
<twig:Divider class="my-6" />
```

## API Reference

::: api-reference
