{%- set _alert_descriptionId = inject('alert.descriptionId') -%}
<twig:Text
    id="{{ _alert_descriptionId }}"
    {{ ... attributes.defaults({
        class: 'mt-2 text-center text-pretty sm:text-left',
    }) }}
>
    {## The description text. -#}
    {{ block(outerBlocks.content) }}
</twig:Text>
