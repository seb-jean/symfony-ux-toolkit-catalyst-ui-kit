{# @block content The description text. #}
{%- set _alert_descriptionId = inject('alert.descriptionId') -%}
<twig:Text
    id="{{ _alert_descriptionId }}"
    class="{{ ('mt-2 text-center text-pretty sm:text-left ' ~ attributes.render('class'))|trim }}"
>
    {{ block(outerBlocks.content) }}
</twig:Text>
